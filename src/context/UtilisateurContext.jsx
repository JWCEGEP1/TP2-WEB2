import { createContext } from "react";

/**
 * Gere les informations de l'utilisateur dans l'app.
 * Partage l'état de l'utilisateur actuel et la liste des utilisateurs
 *
 */

const UtilisateurContext = createContext();
export default UtilisateurContext;